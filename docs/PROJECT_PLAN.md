# План разработки Awesome Sirus Launcher

Документ описывает стадии разработки Electron-приложения для скачивания аддонов с GitHub, обновления клиента World of Warcraft Sirus, установки FPS-патча, бекапа папки `WTF` и запуска игры.

## 1. Цель

Сделать десктопный лаунчер, который:

- хранит путь к папке WoW;
- скачивает и обновляет аддоны из GitHub-репозиториев;
- в будущем умеет автообновлять WeakAuras;
- проверяет файлы клиента по MD5 и докачивает/обновляет отличающиеся файлы;
- ставит FPS-патч в `Data/ruRU/`;
- делает архивный бекап папки `WTF`;
- поддерживает локальные профили нескольких аккаунтов и подставляет выбранный аккаунт в `WTF/Config.wtf` перед запуском;
- умеет проверять и устанавливать обновления самого лаунчера через GitHub Releases;
- запускает игру;
- показывает понятный статус операций, прогресс, ошибки и журнал.

## 2. Рекомендуемый стек

Основной вариант:

- Desktop: `Electron`;
- сборка Electron: `electron-vite`;
- backend/main process: `TypeScript` на Node.js API внутри Electron;
- frontend/renderer: `Vue 3` + `TypeScript`;
- state: `Pinia`;
- UI: сначала свой минимальный дизайн на CSS/Tailwind, UI-kit добавлять только если он реально ускорит работу;
- упаковка: `electron-builder` или связка, которую рекомендует выбранный шаблон `electron-vite`;
- тесты: `Vitest` для core/backend/frontend-модулей, `Playwright` для UI/e2e.
- CI/CD: GitHub Actions для проверки, сборки и будущего релизного autobuild.

Почему так:

- Electron позволяет писать backend и frontend на TypeScript, что ускоряет разработку и уменьшает переключение контекста.
- Node.js-экосистема дает готовые библиотеки для файлов, архивов, скачивания, keychain/credential storage и Windows-сборки.
- `electron-vite` дает быстрый dev loop и поддерживает TypeScript, Vue и Electron main/preload/renderer из коробки.
- Vue 3 хорошо подходит под компактный лаунчер: настройки, списки аддонов, прогресс, модальные окна, журнал.

Альтернатива:

- `Tauri 2` + Vue 3.
- Плюсы: меньше вес и потребление памяти.
- Минусы: backend надо писать на Rust, а это замедлит текущую разработку.

Решение на старте: использовать Electron + Vue 3 + TypeScript. Чтобы позже можно было быстро переехать на Tauri или другой shell, доменную логику держать отдельно от Electron IPC и покрывать тестами.

Источники для выбора стека:

- Electron security: https://electronjs.org/docs/latest/tutorial/security
- Electron context isolation: https://electronjs.org/docs/latest/tutorial/context-isolation
- electron-vite: https://electron-vite.org/
- Tauri 2, для будущего сравнения: https://v2.tauri.app/

## 3. Архитектура модулей

### Main process

Отвечает за системные операции:

- `app-paths` - хранение путей приложения, пользовательских данных, временной папки;
- `settings` - чтение/запись настроек, путь к WoW, выбранные источники, включенные аддоны;
- `secrets` - защищенное хранение GitHub token и паролей аккаунтов через OS credential storage;
- `accounts` - локальные профили аккаунтов, безопасное хранение секрета и запись выбранного аккаунта в `WTF/Config.wtf`;
- `wow-client` - проверка структуры клиента, поиск `Wow.exe`, `Data/ruRU`, `WTF`, запуск игры;
- `downloads` - очередь скачиваний, прогресс, retry, отмена, временные файлы;
- `patches` - получение списка файлов и MD5 из API, сравнение с локальными файлами, обновление клиента;
- `fps-patch` - скачивание `patch-ruRU-[.mpq` и установка в `Data/ruRU/`;
- `addons` - скачивание zip архива кода GitHub-репозитория, распаковка и установка папок аддонов в `Interface/AddOns`;
- `weakauras` - будущий модуль автообновления WeakAuras, источник и формат обновлений расписать позже;
- `backup` - архивирование `WTF`, список бекапов, восстановление;
- `launcher` - запуск игры с проверками;
- `logging` - файл логов и события для UI;
- `versioning` - чтение версии из `VERSION`/`package.json`, отображение версии в UI и синхронизация релизов;
- `app-updater` - проверка версии лаунчера в GitHub Releases, скачивание релизного артефакта и запуск установки обновления.

### Preload

Единственная безопасная граница между UI и системным backend:

- экспортирует typed API через `contextBridge`;
- не отдает renderer прямой доступ к Node.js;
- валидирует входные данные до IPC;
- возвращает DTO, а не внутренние классы.

### Renderer

Пользовательский интерфейс:

- `Dashboard` - общий статус клиента, быстрые действия;
- `Addons` - список аддонов, установка, обновление, удаление;
- `WeakAuras` - будущий экран автообновления WeakAuras, детали расписать позже;
- `ClientUpdate` - проверка MD5, список измененных файлов, прогресс;
- `FpsPatch` - статус патча, установка/переустановка;
- `Backups` - бекапы `WTF`, создание, восстановление, удаление;
- `Accounts` - локальные профили аккаунтов, выбор аккаунта для запуска, добавление/редактирование/удаление;
- `Settings` - путь к WoW, источники API, директории, поведение запуска;
- `GitHubToken` - настройка персонального GitHub token для повышенных лимитов скачивания аддонов;
- `AppUpdate` - статус версии лаунчера, проверка обновлений, прогресс скачивания, установка и перезапуск;
- `Logs` - читаемый журнал операций.

## 4. Источники данных

### MD5 API клиента

Использовать источники с fallback:

1. `https://s-patches.pro/api/client/patches`
2. `https://s-patches.ru/api/client/patches`
3. `https://sirus.world/api/client/patches`

Правила:

- пробовать источники по порядку;
- при ошибке сети или невалидном JSON переходить к следующему;
- кэшировать последний успешный ответ;
- сохранять timestamp проверки;
- не удалять локальные файлы без явного правила из манифеста;
- скачивать только отсутствующие или несовпадающие по MD5 файлы.

### FPS-патч

Основная ссылка:

- `https://d1st4r.ru/patch/patch-ruRU-[.mpq`

Альтернативная ссылка:

- `http://d1st4r.stream/patch/patch-ruRU-[.mpq`

Путь установки:

- `<wowPath>/Data/ruRU/patch-ruRU-[.mpq`

Правила:

- перед установкой проверить, что выбранный путь похож на папку WoW;
- создать `Data/ruRU`, если папки нет, только после подтверждения пользователем;
- скачивать во временный файл;
- после успешного скачивания атомарно перемещать файл в целевой путь;
- показывать размер, прогресс, источник и ошибку при fallback.

### GitHub-аддоны

В MVP аддоны скачивать не из releases/assets, а напрямую как zip архива кода GitHub-репозитория. После скачивания архив распаковывается во временную папку, верхняя папка GitHub-архива отбрасывается, а реальные папки аддонов переносятся в `<wowPath>/Interface/AddOns`.

На первой стадии поддержать конфиг со списком репозиториев:

```json
[
  {
    "id": "example-addon",
    "name": "Example Addon",
    "repo": "owner/repository",
    "ref": "master",
    "installDir": "ExampleAddon",
    "source": "source-zip"
  }
]
```

Правила установки:

- скачивание должно работать без токена, но с ограничениями GitHub rate limit;
- пользователь может указать GitHub personal access token в настройках, чтобы увеличить лимиты;
- токен использовать для GitHub API и скачивания source zip, если GitHub требует/учитывает авторизацию;
- токен хранить только через защищенное хранилище ОС, а не в обычном JSON-config;
- в UI показывать, задан ли токен, но не показывать его значение после сохранения;
- в логах не писать токен, Authorization header или полный URL, если в нем случайно окажется секрет;
- при ошибке 401/403 показывать понятное сообщение: токен неверный, истек, не хватает прав или превышен лимит;
- показывать текущий GitHub rate limit, если API вернул заголовки `X-RateLimit-*`;
- скачивать `https://github.com/{owner}/{repo}/archive/refs/heads/{ref}.zip` или другой zip URL для выбранного `ref`;
- всегда скачивать во временный файл;
- распаковывать во временную директорию;
- не копировать верхнюю папку вида `{repo}-{branch}` целиком в `AddOns`;
- искать внутри распакованного архива папки, где есть `.toc`;
- если `installDir` указан, ставить найденную папку под этим именем;
- если в архиве несколько папок аддонов, переносить каждую папку с `.toc` наверх в `Interface/AddOns`;
- перед заменой существующего аддона делать временный backup или atomic replace;
- защищаться от zip slip и путей за пределы временной директории.

Позже добавить:

- несколько папок внутри одного архива;
- pinned commit/tag;
- ручной zip URL;
- проверку установленной версии по `.toc`.

### Автообновление лаунчера

Механика будущего автообновления самого приложения:

- локальная версия берется из `package.json.version`, синхронизированного с `VERSION`;
- удаленная версия берется из GitHub Releases выбранного репозитория;
- версия сравнивается по SemVer;
- pre-release версии игнорируются по умолчанию, если пользователь не включил канал тестовых обновлений;
- при наличии новой версии UI показывает changelog/release notes и размер загрузки;
- пользователь подтверждает скачивание и установку;
- лаунчер скачивает release asset во временную папку;
- после скачивания проверяет размер и checksum/signature, если они опубликованы;
- установка запускается только после успешной проверки;
- перед установкой лаунчер завершает активные операции или просит пользователя дождаться их завершения;
- после установки приложение перезапускается.

Предпочтительный путь для Electron:

- использовать `electron-builder` + `electron-updater` и GitHub Releases как provider;
- в GitHub Actions публиковать installer/portable и update metadata;
- держать manual fallback: открыть страницу релиза в браузере, если автоустановка не сработала.

Открытые вопросы перед реализацией:

- какие артефакты публикуем для Windows: installer, portable или оба;
- нужен ли checksum-файл отдельно;
- нужен ли code signing certificate;
- нужен ли канал `beta`/`nightly`;
- обновлять ли приложение автоматически при старте или только по кнопке.

## 5. Стадии разработки

### Стадия 0. Подготовка проекта

Результат: запускаемый пустой Electron/Vue проект.

- создать проект на `electron-vite` + Vue + TypeScript;
- добавить `package.json` и синхронизировать `package.json.version` с `VERSION`;
- настроить ESLint, Prettier, Vitest;
- настроить базовые alias;
- завести структуру `main`, `preload`, `renderer`, `shared`, `core`;
- добавить CI-скрипты локальной проверки;
- подключить GitHub Actions build workflow;
- завести `CHANGELOG.md` и правило обязательного обновления changelog агентами;
- описать команды в `README.md`.

### Стадия 1. Каркас приложения

Результат: UI с навигацией и безопасным IPC.

- сделать главное окно;
- включить `contextIsolation`;
- отключить прямой Node.js доступ в renderer;
- сделать typed IPC контракт;
- добавить страницы Dashboard, Addons, Update, Backups, Settings, Logs;
- добавить общий store состояния операций.

### Стадия 2. Настройки и путь к WoW

Результат: приложение знает, где лежит игра.

- выбрать папку через системный dialog;
- проверить наличие `Wow.exe`, `Data`, `Interface`, `WTF`;
- сохранить путь в user data;
- показать статус валидности клиента;
- дать пользователю изменить путь;
- добавить тесты на валидацию путей.

### Стадия 3. Бекап папки WTF

Результат: безопасное создание и восстановление бекапов.

- архивировать `<wowPath>/WTF` в `.zip`;
- хранить бекапы в user data или выбранной папке;
- имя файла: `wtf-backup-YYYY-MM-DD-HH-mm-ss.zip`;
- показывать список бекапов;
- восстановление делать через временную папку и подтверждение;
- перед восстановлением создавать safety backup текущей `WTF`;
- добавить обработку больших папок и прогресс.

### Стадия 4. FPS-патч

Результат: установка патча в один клик.

- скачать патч по основной ссылке;
- при ошибке использовать альтернативную ссылку;
- сохранить в `<wowPath>/Data/ruRU/`;
- показывать текущий статус установлен/не установлен;
- добавить переустановку;
- логировать источник скачивания.

### Стадия 5. Проверка и обновление клиента по MD5

Результат: лаунчер умеет находить отличающиеся файлы клиента.

- получить манифест файлов из API;
- нормализовать пути под Windows;
- посчитать MD5 локальных файлов потоково;
- сравнить с манифестом;
- показать список отсутствующих/битых файлов;
- скачать нужные файлы во временную папку;
- проверить MD5 после скачивания;
- переместить файлы в клиент;
- поддержать retry и отмену.

Открытый вопрос: API может возвращать не только список MD5, но и URL/пути скачивания. Перед реализацией надо сохранить пример ответа и описать точную схему.

### Стадия 6. Аддоны с GitHub

Результат: установка и обновление аддонов.

- завести локальный каталог доступных аддонов;
- добавить настройку GitHub token для повышенных лимитов;
- хранить GitHub token через защищенное хранилище ОС;
- использовать токен в GitHub-запросах через `Authorization: Bearer ...`;
- показывать rate limit и ошибки токена без утечки секрета;
- построить zip URL архива кода GitHub по `repo` и `ref`;
- скачать zip архива кода во временный файл;
- распаковать во временную папку;
- найти папки с `.toc`;
- отбросить верхнюю папку GitHub-архива и перенести папки аддонов наверх;
- установить в `<wowPath>/Interface/AddOns`;
- показывать установленную версию;
- добавить удаление аддона;
- добавить обновление всех аддонов.

### Стадия 7. Запуск игры

Результат: кнопка запуска WoW.

- определить исполняемый файл;
- запустить процесс через Electron main;
- не блокировать UI;
- показать ошибку, если файл не найден;
- опционально закрывать лаунчер после запуска;
- опционально запускать проверку клиента перед игрой.

### Стадия 8. Мультиаккаунты

Результат: пользователь может выбрать локально сохраненный аккаунт перед запуском игры.

- добавить экран/раздел `Accounts`;
- хранить список профилей аккаунтов локально;
- хранить логин в локальном config, а пароль в защищенном хранилище ОС, если выбранная библиотека это поддерживает;
- если защищенное хранилище недоступно, явно предупреждать пользователя перед сохранением пароля;
- перед запуском игры обновлять `<wowPath>/WTF/Config.wtf`;
- менять или добавлять строки:
  - `SET accountName "login"`
  - `SET readTerminationWithoutNotice "password"`
- пример целевого файла для текущей установки: `F:\games\sirus\World of Warcraft Sirus\WTF\Config.wtf`;
- не хардкодить этот путь, а строить его от сохраненного `<wowPath>`;
- перед изменением `Config.wtf` создавать быстрый safety backup;
- не показывать пароль в UI, логах, ошибках и changelog;
- добавить тесты на обновление `Config.wtf` без потери остальных строк.

Открытый вопрос: строка `SET readTerminationWithoutNotice` выглядит как нестандартное место для пароля. Перед реализацией надо проверить, что именно клиент Sirus ожидает пароль в этой строке, и не логировать ее значение.

### Стадия 9. Автообновление WeakAuras

Результат: будущий модуль запланирован, детали реализации будут расписаны отдельно.

- определить источник обновлений WeakAuras;
- понять формат хранения и установки WeakAuras для целевой версии WoW/Sirus;
- решить, обновляем все ауры автоматически или только выбранные пользователем;
- добавить бекап перед изменением файлов WeakAuras;
- продумать конфликт ручных изменений пользователя и удаленных обновлений;
- расписать отдельный подробный план перед реализацией.

### Стадия 10. Автообновление лаунчера

Результат: лаунчер умеет проверять GitHub Releases и обновлять себя.

- настроить публикацию релизных артефактов в GitHub Releases;
- выбрать `electron-updater` или собственный GitHub Releases checker;
- добавить модуль `app-updater` в backend;
- добавить экран/виджет `AppUpdate` в Settings или Dashboard;
- сравнивать локальную и удаленную версии по SemVer;
- показывать release notes перед установкой;
- скачивать релизный asset во временную папку с прогрессом;
- проверять checksum/signature, если они доступны;
- запускать установку и перезапуск приложения;
- добавить manual fallback на страницу GitHub Release;
- покрыть тестами выбор версии, парсинг GitHub Release, SemVer compare и fallback-сценарии.

### Стадия 11. Полировка и релиз

Результат: приложение можно отдавать пользователям.

- добавить обработку ошибок и понятные тексты;
- добавить журнал операций;
- добавить темную/светлую тему, если нужно;
- добавить e2e smoke tests;
- настроить сборку Windows installer/portable;
- настроить релизную сборку GitHub Actions при изменении `VERSION`;
- настроить публикацию релиза и update metadata для автообновления;
- подписать релиз, если будет сертификат;
- подготовить страницу релизов и инструкцию установки.

## 6. Портируемость и тесты для будущего переписывания

Цель: если позже решим перейти с Electron на Tauri или другой desktop shell, переписывать только shell-адаптеры, а не бизнес-логику.

Правила архитектуры:

- доменную логику держать в `core`, без импорта `electron`, `BrowserWindow`, `ipcMain`, `dialog`;
- `main` вызывает `core` через тонкие adapters/services;
- `preload` только пробрасывает typed API, не содержит бизнес-логики;
- renderer вызывает backend через typed client, а не напрямую через разрозненные IPC-строки;
- файловую систему, сеть, архивы, keychain и запуск процесса прятать за interfaces;
- для каждого внешнего адаптера иметь mock/fake implementation в тестах;
- DTO и ошибки хранить в `shared`, чтобы их можно было переиспользовать при миграции.

Обязательные тесты:

- unit tests для `core`: пути WoW, MD5 manifest, backup plan, addon install plan, `Config.wtf` patching, fallback источников;
- adapter contract tests: filesystem, downloader, archive extractor, process launcher, secret storage;
- integration tests на временных директориях для backup/restore, addon zip install, FPS patch install, `Config.wtf` update;
- renderer tests для stores/composables без привязки к Electron;
- e2e smoke tests через Playwright только для сквозных пользовательских сценариев.

Минимальное правило покрытия: новая функция сначала получает тестируемый `core`-модуль, потом Electron adapter и UI.

## 7. Версионирование, changelog и autobuild

Правила:

- `CHANGELOG.md` хранит все заметные изменения.
- Агент после изменения проекта обновляет `CHANGELOG.md` в разделе `[Unreleased]`.
- `VERSION` хранит текущую релизную версию.
- После появления `package.json` поле `package.json.version` должно совпадать с `VERSION`.
- Обычные изменения не обязаны повышать версию.
- Релиз начинается с отдельного изменения `VERSION` и переноса записей из `[Unreleased]` в новую версию.
- GitHub Release tag должен совпадать с версией, например `v1.2.3`.
- Автообновление лаунчера проверяет GitHub Releases и сравнивает remote version с локальной.

GitHub Actions:

- PR должен запускать проверку, тесты и обычную сборку, когда появится Electron-проект.
- Push в `main`/`master` с изменением `VERSION` должен запускать релизную сборку.
- До появления `package.json` workflow должен пропускать Node/Electron шаги, чтобы плановая инфраструктура не ломала репозиторий.
- После появления packaging config workflow должен сохранять Windows installer/portable как artifacts.
- Для релизов workflow должен публиковать artifacts в GitHub Releases и генерировать update metadata для `electron-updater`, если выбран этот путь.

## 8. Минимальный MVP

Чтобы быстро получить полезный результат:

1. Electron/Vue каркас.
2. Настройка пути к WoW.
3. Бекап `WTF`.
4. Установка FPS-патча.
5. Запуск игры.
6. Потом мультиаккаунты.
7. Потом MD5 обновления.
8. Потом GitHub-аддоны.
9. Потом автообновление WeakAuras.
10. Потом автообновление самого лаунчера.

## 9. Риски

- У API патчей может быть нестабильная или недокументированная схема.
- HTTP fallback для FPS-патча не шифруется, его стоит использовать только как резервный источник.
- GitHub API имеет rate limits без токена.
- GitHub token повышает лимиты, но его утечка опасна; хранить и логировать его нужно как секрет.
- Антивирусы могут подозрительно относиться к лаунчерам, которые скачивают файлы и запускают `.exe`.
- Восстановление `WTF` может перезаписать настройки пользователя, поэтому нужен safety backup.
- Сохранение паролей локально несет риск компрометации; нужен защищенный storage и запрет вывода секретов в логи.
- Неправильная правка `Config.wtf` может сломать пользовательские настройки, поэтому нужен backup и точечная замена только нужных строк.
- Если смешать Electron API с доменной логикой, будущий переход на Tauri станет дорогим; это предотвращается core/adapters архитектурой и тестами.
- Autobuild не будет выпускать полноценные артефакты, пока не появятся `package.json`, build script и packaging config.
- Автообновление может сломаться без корректных GitHub Release assets, update metadata или code signing.
- Приватный GitHub-репозиторий усложнит автообновление из-за доступа к release assets.

## 10. Решения, которые надо принять позже

- Какие минимальные scopes нужны для GitHub token, если аддоны будут скачиваться только из публичных репозиториев.
- Где хранить каталог аддонов: в репозитории, remote JSON или пользовательский список.
- Какой источник и формат использовать для автообновления WeakAuras.
- Использовать `electron-updater` или сначала сделать простой GitHub Releases checker с ручной установкой.
- Делать ли релизный build строго только по изменению `VERSION` или также по изменению `package.json.version`.
- Публиковать ли artifacts только в workflow или автоматически создавать GitHub Release.
- Какие имена assets и update metadata использовать для автообновления Windows.
- Нужна ли поддержка нескольких установок WoW.
- Какую библиотеку использовать для защищенного локального хранения паролей аккаунтов.
- Нужно ли разрешать аккаунты без сохранения пароля, только с автоподстановкой логина.
- Нужно ли проверять подписи или контрольные суммы FPS-патча.
